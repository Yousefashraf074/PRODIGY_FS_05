# Kubernetes Secret for Backend
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  labels:
    app: connectsphere-backend
  namespace: default
stringData:
  DATABASE_URL: postgresql://postgres:password@postgres:5432/connectsphere
  JWT_SECRET: supersecretkey
